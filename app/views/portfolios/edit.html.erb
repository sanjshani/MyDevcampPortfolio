<h1>Edit a Portfolio item</h1>

<%= render 'form', portfolio_item: @portfolio_item %>

<%# Delete everything below!!!!
<%= form_with(model: @portfolio_item, local: true) do |form| %>     <%#code copied from views/blogs/new.html.erb!!!
<%#= form_for(@portfolio_item) do |form| % >                           Tutorial version!!!

Though they are different, Both codes worked in new.html.erb!!! 
Maybe something to do with Rails version??? % >
  <div class="field">
    <%= form.label :title % >
    <%= form.text_field :title % >
  </div>

  <div class="field">
    <%= form.label :subtitle % >
    <%= form.text_field :subtitle % >
  </div>

  <div class="field">
    <%= form.label :body % >
    <%= form.text_area :body % >
  </div>

  <div class="actions">
    <%= form.submit % >
  </div>
<% end %>


